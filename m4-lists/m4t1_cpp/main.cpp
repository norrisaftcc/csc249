#include <iostream>
using namespace std;

/* C++ version of this:
    # add five nodes at the head
    head = None

    for count in range(1, 6):
        head = Node(count, head)
    # print contents
    probe = head
    while probe != None:
        print(probe.data)
        probe = probe.next

*/

/*
Purpose of M4T1:
- Add five nodes to the head of an empty list
- Iterate through the list, printing each node
*/

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
